services:
  - type: web
    name: venue-webhook-2
    env: python
    plan: free
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: uvicorn main:app --host=0.0.0.0 --port=$PORT
    autoDeploy: true
